import {citylist_api} from "../../api/city"

const state = {}
const mutations = {}
const actions = {
    handleGetCityAction(commit){
        console.log(data)
    }
}

export default {
    state,
    mutations,
    actions,
    namespace:true
}